# Feast configuration for the local SQLite workflow.
# This assignment uses a local backend only, so the offline store is the CSV file
# and the online store is a local SQLite database.

project: iris_feature_store
registry: data/registry.db
provider: local

offline_store:
  type: file

online_store:
  type: sqlite
  path: data/online_store.db
